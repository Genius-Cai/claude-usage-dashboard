# Claude Code Usage Dashboard Environment Variables

# API Configuration (required for production with backend)
NEXT_PUBLIC_API_URL=http://localhost:8000/api
NEXT_PUBLIC_WS_URL=ws://localhost:8000/ws

# Demo Mode - Set to 'true' for frontend-only deployment (no backend needed)
# Uses mock data for all visualizations
NEXT_PUBLIC_USE_MOCK_DATA=false

# PWA Configuration (optional)
NEXT_PUBLIC_APP_NAME=Claude Usage Dashboard
NEXT_PUBLIC_APP_SHORT_NAME=Claude Usage
